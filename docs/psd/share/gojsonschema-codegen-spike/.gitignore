# Generated by `go generate ./...` — see gen.go. Deliberately not committed:
# the two files are ~8,700 lines of DO-NOT-EDIT output that nobody reads, they
# are reproducible exactly from the pinned tool in go.mod, and half the point of
# this spike is that they are a build artifact of the schemas rather than source.
#
# Run `go generate ./...` before `go test ./...`.
character/character.gen.go
baseline/baseline.gen.go
additionalprops/additionalprops.gen.go
